XOXO Messenger V2 🖤
Web-мессенджер в стиле Cyberpunk/Neon с 3D-аватарами и шейдерными эффектами.
Полностью переписанная архитектура (V2): модульная структура, Vanilla JS (ES Modules), Firebase BaaS.
🛠 Технический стек
Frontend: HTML5, CSS3 (Flexbox/Grid), Vanilla JavaScript (ES6+ Modules).
3D Graphics: THREE.js + GLSL Shaders (для жидких аватарок).
Animations: GSAP (GreenSock).
Backend: Firebase (Authentication, Firestore Database).
Сборка: Отсутствует (Native ES Modules через CDN).
📂 Структура проекта и назначение файлов
Здесь описано, за что отвечает каждый файл. Для будущих AI: читайте это перед внесением изменений.
1. Корневые файлы
index.html — Страница входа (Login). Содержит форму авторизации и фоновые анимации.
chat.html — Основной интерфейс мессенджера. Сайдбар, область чата, WebGL-контейнер для аватара. Подключает стили app.css и скрипт js/app.js.
2. Стили (css/)
css/login.css — Стили только для страницы входа. Анимация фоновых пятен (blobs), глассморфизм.
css/app.css — ГЛАВНЫЙ ФАЙЛ СТИЛЕЙ ЧАТА.
Содержит CSS Variables (:root).
Отвечает за Layout (.app-layout, .sidebar, .chat-area).
Важно: Содержит фикс flex-shrink: 0 для сообщений, предотвращающий их растягивание.
3. Логика JavaScript (js/)
Конфигурация и Вход
js/config.js — Инициализация Firebase. Экспортирует объекты auth и db для использования в других файлах. Содержит API ключи.
js/login.js — Логика страницы index.html. Обработка нажатия кнопки "Войти", валидация полей, переадресация на chat.html.
js/app.js — Точка входа приложения (Main Entry).
Проверяет авторизацию (если нет — кидает на логин).
Загружает профиль пользователя.
Инициализирует UI чата (ChatUI) и 3D-аватар (AvatarRenderer).
Сервисы (js/services/) — Работа с данными
js/services/auth.js — Обертка над Firebase Auth. Методы: login, logout, monitor (слежка за состоянием), getCurrentUser.
js/services/database.js — Обертка над Firestore.
getProfile: Получает данные юзера или создает дефолтные.
subscribeToMessages: Realtime-подписка на сообщения (onSnapshot).
sendMessage: Отправка данных в коллекцию messages.
UI и Рендеринг (js/ui/) — Работа с DOM
js/ui/chat-ui.js — Класс ChatUI.
Отвечает за отрисовку сообщений в HTML.
Создает DOM-элементы (div.message-row).
Обрабатывает клик по кнопке "Отправить" и Enter.
Защищает от XSS (функция escapeHtml).
Ядро Графики (js/core/) — WebGL
js/core/avatar.js — Класс AvatarRenderer (THREE.js). Создает сцену, камеру и PlaneMesh с шейдером. Обрабатывает наведение мыши (GSAP анимация искажения).
js/core/shaders.js — Хранит код GLSL шейдеров (Vertex + Fragment) и настройки переходов (Liquid, Glitch и т.д.).
🤖 База знаний для Нейросетей (AI Context)
Если ты — AI (ChatGPT, Claude, Copilot), который будет дорабатывать этот проект, следуй этим правилам:
Архитектура: Проект использует ES Modules (import ... from ...). Не используй require() и не пытайся добавить npm install, если пользователь явно не попросит перевести проект на Node.js/Vite.
CSS Flexbox Fix: В файле css/app.css классы .messages-list, .message-row и .bubble настроены очень строго.
Родитель: flex-direction: column.
Дети (.message-row): flex-shrink: 0.
Не меняй display: flex на block в строках сообщений, это сломает выравнивание аватарок.
Не добавляй height: 100% сообщениям.
Firebase: Используется Firebase Web SDK v10 (Modular). Все импорты должны быть через CDN (https://www.gstatic.com/...).
Безопасность: В chat-ui.js всегда используй экранирование HTML перед вставкой текста пользователя.
3D: Аватарки — это не <img>, а Canvas (WebGL). Если нужно изменить картинку, используй метод загрузки текстур в AvatarRenderer, а не меняй src в HTML.
🚀 Как запустить
Клонировать репозиторий.
Открыть файл index.html в браузере (через Live Server или просто двойным кликом, но лучше через локальный сервер, чтобы избежать CORS проблем с модулями).
Войти (любой логин, пароль не важен в текущей демо-версии, но должен быть не пустым).
XOXO Messenger V2 — Code is poetry, styles are neon.
